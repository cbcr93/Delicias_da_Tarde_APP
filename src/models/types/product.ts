export type ProductsEntities = {
  id: string;
  name: string;
  description: string;
  price: number;
  type: string;
  code: string;
  amount: number;
};

export type IProduct = {
  id: string;
  name: string;
  description: string;
  price: number;
  type: string;
  code: string;
  amount: number;
};
